export const SCREEN_COLS = 50;
export const SCREEN_ROWS = 50;

// width and height of each grid location (table cell) in pixels
export const PARTICLE_SIZE = 8;

export const OUT_OF_BOUNDS = -1;

export const FRAME_TIMING_SIZE = 30;